name: 'nginx'
networks:
  homelab_network:
    external: true

services:
  web:
    image: 'nginx:1.29.3-alpine'
    networks:
      - homelab_network
  php-fpm:
    image: 'php:8.4-fpm-alpine
'
    networks:
      - homelab_network
